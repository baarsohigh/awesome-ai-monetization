import { mkdir, writeFile } from "node:fs/promises";
import { resolve } from "node:path";
import { records, root } from "./lib.mjs";

const data = await records();
const json = JSON.stringify(data).replaceAll("<", "\\u003c");
const html = `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Awesome AI Monetization</title><meta name="description" content="The open-source database of how AI products make money."><style>
:root{--bg:#f4f1e9;--ink:#151713;--muted:#62695c;--line:#d7d2c6;--acid:#dff07c}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--ink);font:15px system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}main{max-width:1180px;margin:auto;padding:28px}nav{display:flex;justify-content:space-between;font-size:13px;font-weight:700}.brand{letter-spacing:-.08em;font-size:19px}header{padding:88px 0 64px;border-bottom:1px solid var(--line)}.eyebrow{font:11px ui-monospace,SFMono-Regular,Menlo,monospace;letter-spacing:.08em;color:#61705a}h1{font-size:clamp(54px,9vw,124px);letter-spacing:-.075em;line-height:.86;margin:17px 0}h1 em{font-family:Georgia,serif;font-weight:400}.lede{max-width:560px;color:var(--muted);line-height:1.65}.stats{display:grid;grid-template-columns:repeat(4,1fr);border-bottom:1px solid var(--line)}.stats div{padding:24px 0;border-right:1px solid var(--line);padding-left:22px}.stats div:first-child{padding-left:0}.stats div:last-child{border:0}.stats b{display:block;font-size:30px;letter-spacing:-.06em}.stats small{color:var(--muted)}section{padding:70px 0}.toolbar{display:flex;border:1px solid var(--line);background:#fffdf8}.toolbar input{width:100%;border:0;padding:15px;background:transparent;font:inherit;outline:0}.filters{display:flex;gap:8px;flex-wrap:wrap;margin:15px 0}.filters button,button{cursor:pointer;background:transparent;border:1px solid var(--line);padding:8px 10px;border-radius:16px;font-size:12px}.filters button.active{background:#253020;border-color:#253020;color:#fff}.count{color:var(--muted);font:11px ui-monospace,monospace}.grid{display:grid;grid-template-columns:repeat(2,1fr);margin-top:12px;border-top:1px solid var(--line);border-left:1px solid var(--line)}article{background:#fbfaf6;border-right:1px solid var(--line);border-bottom:1px solid var(--line);padding:22px;min-height:220px}article h3{margin:4px 0 14px;font-size:22px;letter-spacing:-.04em}article p{color:var(--muted);font-size:13px;line-height:1.55}.tags span{display:inline-block;border:1px solid var(--line);padding:4px 6px;margin:0 4px 5px 0;font:9px ui-monospace,monospace;text-transform:uppercase}.open{color:#426319;border:0;padding:0;margin-top:10px}.modal{position:fixed;inset:0;background:#15171388;display:none;justify-content:flex-end}.modal.show{display:flex}.panel{width:min(650px,100%);overflow:auto;background:#f8f6ef;padding:30px}.panel h2{font-size:38px;letter-spacing:-.06em}.evidence{margin:16px 0;padding:18px;border:1px solid var(--line)}.observed{border-left:4px solid #7b9e31;background:#fffdf7}.analysis{border-left:4px solid #78806e;background:#e9eee1}.sources a{display:block;border-top:1px solid var(--line);padding:12px 0;color:#426319;font-size:13px}@media(max-width:650px){main{padding:20px}.stats,.grid{grid-template-columns:1fr 1fr}.stats div{border-bottom:1px solid var(--line)}.grid{grid-template-columns:1fr}header{padding:60px 0}.stats b{font-size:25px}}
</style></head><body><main><nav><span class="brand">AAM●</span><a href="https://github.com/baarsohigh/awesome-ai-monetization">GitHub ↗</a></nav><header><p class="eyebrow">OPEN-SOURCE AI MONETIZATION DATABASE</p><h1>How AI products<br><em>make money.</em></h1><p class="lede">Source-backed research on AI pricing architecture, charge metrics, value metrics, and revenue expansion mechanics.</p></header><div class="stats" id="stats"></div><section><p class="eyebrow">EXPLORE THE DATABASE</p><h2>Commercial architectures, not just price points.</h2><div class="toolbar"><input id="search" placeholder="Search company, product, pricing component, or metric…"></div><div class="filters" id="filters"></div><p class="count" id="count"></p><div class="grid" id="grid"></div></section></main><div class="modal" id="modal"><div class="panel" id="panel"></div></div><script>const records=${json};const label=s=>s.replaceAll('-', ' ');const components=['usage-based','credit-based','seat-based','outcome-based','freemium','enterprise-contract'];let active='',query='';const grid=document.querySelector('#grid'),modal=document.querySelector('#modal'),panel=document.querySelector('#panel');document.querySelector('#stats').innerHTML=[['records',records.length],['pricing components',new Set(records.flatMap(x=>x.classification.pricing_components)).size],['charge metrics',new Set(records.flatMap(x=>x.classification.charge_metrics)).size],['with sources','100%']].map(x=>'<div><b>'+x[1]+'</b><small>'+x[0]+'</small></div>').join('');document.querySelector('#filters').innerHTML=['all',...components].map(x=>'<button data-filter="'+x+'">'+label(x)+'</button>').join('');function render(){const found=records.filter(r=>JSON.stringify(r).toLowerCase().includes(query.toLowerCase())&&(!active||r.classification.pricing_components.includes(active)));document.querySelector('#count').textContent=found.length+' records matching · sources checked per record';grid.innerHTML=found.map(r=>'<article><span class="eyebrow">'+r.company+'</span><h3>'+r.product+'</h3><div class="tags">'+r.classification.pricing_components.slice(0,3).map(t=>'<span>'+label(t)+'</span>').join('')+'</div><p>'+r.analysis.monetization_summary+'</p><button class="open" data-id="'+r.id+'">View research →</button></article>').join('');document.querySelectorAll('[data-filter]').forEach(b=>b.classList.toggle('active',(b.dataset.filter==='all'&&!active)||b.dataset.filter===active));}document.querySelector('#search').oninput=e=>{query=e.target.value;render()};document.querySelector('#filters').onclick=e=>{if(e.target.dataset.filter){active=e.target.dataset.filter==='all'?'':e.target.dataset.filter;render()}};grid.onclick=e=>{const id=e.target.dataset.id;if(!id)return;const r=records.find(x=>x.id===id);panel.innerHTML='<button id="close">Close ×</button><p class="eyebrow">RESEARCH RECORD · '+r.review_status+'</p><h2>'+r.product+'</h2><p>'+r.company+' · '+r.domain+'</p><div class="evidence observed"><b>Observed & sourced</b><p>'+r.observed.pricing_architecture+'</p><p><b>Charge metrics:</b> '+r.classification.charge_metrics.map(label).join(', ')+'</p></div><div class="evidence analysis"><b>Analyst interpretation</b><p>'+r.analysis.monetization_summary+'</p><p><b>Expansion:</b> '+r.analysis.expansion_vectors.map(x=>label(x.type)).join(', ')+'</p></div><div class="sources"><p class="eyebrow">PRIMARY SOURCES</p>'+r.sources.map(s=>'<a href="'+s.url+'" target="_blank">'+s.title+' ↗<br><small>'+s.source_type+' · accessed '+s.accessed_at+'</small></a>').join('')+'</div>';modal.classList.add('show')};modal.onclick=e=>{if(e.target===modal||e.target.id==='close')modal.classList.remove('show')};render();</script></body></html>`;
const output = resolve(root, "gh-pages");
await mkdir(output, { recursive: true });
await writeFile(resolve(output, "index.html"), html);
console.log(`Built static GitHub Pages site with ${data.length} records`);
